# Disabled for CTECH fork.
name: Auto-assign PR to Author

on:
  workflow_dispatch:

jobs:
  auto-assign:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

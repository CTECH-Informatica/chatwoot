# Disabled for CTECH fork — upstream thread locking automation.
name: 'Lock Threads'

on:
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

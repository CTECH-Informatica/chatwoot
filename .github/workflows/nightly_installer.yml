# Disabled for CTECH fork — upstream nightly Linux installer test.
name: Run Linux nightly installer

on:
  workflow_dispatch:

jobs:
  test:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

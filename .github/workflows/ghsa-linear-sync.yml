# Disabled for CTECH fork — upstream Linear security sync.
name: Sync GHSA advisories to Linear

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

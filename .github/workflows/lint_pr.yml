# Disabled for CTECH fork — semantic PR title validation for upstream OSS.
name: "Lint PR"

on:
  workflow_dispatch:

permissions:
  pull-requests: read

jobs:
  main:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

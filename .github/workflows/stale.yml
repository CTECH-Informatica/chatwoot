# Disabled for CTECH fork — upstream stale issue/PR automation.
name: Mark stale issues and pull requests

on:
  workflow_dispatch:

jobs:
  stale:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"

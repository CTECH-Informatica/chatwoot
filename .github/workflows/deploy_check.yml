# Disabled for CTECH fork — upstream Heroku review-app check.
name: Deploy Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment_check:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for CTECH fork"
